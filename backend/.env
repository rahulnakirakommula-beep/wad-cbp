# Server
PORT=5005
NODE_ENV=development

# Database
MONGODB_URI=mongodb://127.0.0.1:27017/bluepenguin_db

# JWT
JWT_SECRET=bUFGidHJEdrAu5JlJCHoB5

# URLs (required for email verification links and redirects)
BACKEND_URL=http://localhost:5005
FRONTEND_URL=http://localhost:5176

# Redis (optional in local dev — feed caching disabled without this)
# REDIS_URL=redis://localhost:6379

# SMTP Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=igiveheadsup@gmail.com
SMTP_PASS=tzei ajuf nike zmua
SMTP_FROM=COA Notifications <igiveheadsup@gmail.com>

# For development reference:
# SMTP_HOST=smtp.mailtrap.io
# SMTP_PORT=2525
